{
  "name": "flow-diff",
  "displayName": "Flow Diff",
  "description": "Side-by-side git diff viewer with center-gutter connectors",
  "version": "0.1.0",
  "license": "MIT",
  "publisher": "RiccardoFilippozzi",
  "repository": "https://github.com/riccardoflp/flow-diff",
  "engines": {
    "vscode": "^1.90.0"
  },
  "icon": "resources/icon/icon.png",
  "categories": [
    "Other",
    "SCM Providers"
  ],
  "keywords": [
    "diff",
    "git",
    "side-by-side",
    "webstorm",
    "compare"
  ],
  "extensionDependencies": [
    "vscode.git"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "flowDiff.openDiff",
        "title": "Open Diff (Working Tree vs HEAD)",
        "category": "Flow Diff",
        "icon": "$(diff)"
      },
      {
        "command": "flowDiff.openDiffStaged",
        "title": "Open Diff (Index vs HEAD)",
        "category": "Flow Diff"
      },
      {
        "command": "flowDiff.nextChunk",
        "title": "Next Change",
        "category": "Flow Diff",
        "icon": "$(arrow-down)"
      },
      {
        "command": "flowDiff.prevChunk",
        "title": "Previous Change",
        "category": "Flow Diff",
        "icon": "$(arrow-up)"
      },
      {
        "command": "flowDiff.openFile",
        "title": "Open File",
        "category": "Flow Diff",
        "icon": "$(go-to-file)"
      },
      {
        "command": "flowDiff.stageFile",
        "title": "Stage File",
        "category": "Flow Diff",
        "icon": "$(add)"
      },
      {
        "command": "flowDiff.unstageFile",
        "title": "Unstage File",
        "category": "Flow Diff",
        "icon": "$(remove)"
      },
      {
        "command": "flowDiff.revertFile",
        "title": "Discard Changes (Revert File)",
        "category": "Flow Diff",
        "icon": "$(discard)"
      }
    ],
    "menus": {
      "scm/resourceState/context": [
        {
          "command": "flowDiff.openDiff",
          "when": "scmProvider == git",
          "group": "navigation@1"
        },
        {
          "command": "flowDiff.openDiffStaged",
          "when": "scmProvider == git",
          "group": "navigation@2"
        }
      ],
      "editor/title": [
        {
          "command": "flowDiff.openDiff",
          "when": "config.git.enabled && resourceScheme == file && flowDiff.activeFileHasChanges",
          "group": "navigation@-1"
        },
        {
          "command": "flowDiff.openFile",
          "when": "activeWebviewPanelId == flowDiff.panel",
          "group": "navigation@1"
        },
        {
          "command": "flowDiff.prevChunk",
          "when": "activeWebviewPanelId == flowDiff.panel",
          "group": "navigation@2"
        },
        {
          "command": "flowDiff.nextChunk",
          "when": "activeWebviewPanelId == flowDiff.panel",
          "group": "navigation@3"
        },
        {
          "command": "flowDiff.stageFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == worktree",
          "group": "navigation@4"
        },
        {
          "command": "flowDiff.revertFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == worktree",
          "group": "navigation@5"
        },
        {
          "command": "flowDiff.unstageFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == index",
          "group": "navigation@4"
        }
      ],
      "commandPalette": [
        {
          "command": "flowDiff.nextChunk",
          "when": "activeWebviewPanelId == flowDiff.panel"
        },
        {
          "command": "flowDiff.prevChunk",
          "when": "activeWebviewPanelId == flowDiff.panel"
        },
        {
          "command": "flowDiff.openFile",
          "when": "activeWebviewPanelId == flowDiff.panel"
        },
        {
          "command": "flowDiff.stageFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == worktree"
        },
        {
          "command": "flowDiff.unstageFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == index"
        },
        {
          "command": "flowDiff.revertFile",
          "when": "activeWebviewPanelId == flowDiff.panel && flowDiff.activeSide == worktree"
        }
      ]
    },
    "configuration": {
      "title": "Flow Diff",
      "properties": {
        "flowDiff.interceptGitOpenChange": {
          "type": "boolean",
          "default": true,
          "description": "Use Flow Diff instead of VS Code's built-in diff editor for git diffs opened from the Source Control panel, the commit graph, or GitLens."
        }
      }
    },
    "keybindings": [
      {
        "command": "flowDiff.nextChunk",
        "key": "f7",
        "when": "activeWebviewPanelId == flowDiff.panel"
      },
      {
        "command": "flowDiff.prevChunk",
        "key": "shift+f7",
        "when": "activeWebviewPanelId == flowDiff.panel"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./ && tsc -p src/webview && npm run bundle:webview",
    "bundle:webview": "esbuild src/webview/main.ts --bundle --outdir=out/webview --format=esm --splitting --chunk-names=chunks/[name]-[hash] --loader:.ttf=file --sourcemap && esbuild node_modules/monaco-editor/esm/vs/editor/editor.worker.js --bundle --format=iife --outfile=out/webview/editor.worker.js --minify",
    "watch": "tsc -watch -p ./",
    "watch:webview": "esbuild src/webview/main.ts --bundle --outdir=out/webview --format=esm --splitting --chunk-names=chunks/[name]-[hash] --loader:.ttf=file --sourcemap --watch",
    "test": "npm run compile && node --test \"out/test/**/*.test.js\"",
    "package": "vsce package"
  },
  "dependencies": {
    "diff": "^7.0.0"
  },
  "devDependencies": {
    "@shikijs/engine-javascript": "^4.2.0",
    "@shikijs/langs": "^4.2.0",
    "@shikijs/monaco": "^4.2.0",
    "@shikijs/themes": "^4.2.0",
    "@types/diff": "^7.0.0",
    "@types/node": "20.x",
    "@types/vscode": "1.90.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.24.0",
    "monaco-editor": "^0.55.1",
    "shiki": "^4.2.0",
    "typescript": "^5.4.0"
  }
}
